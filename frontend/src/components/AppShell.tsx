import { Link, NavLink, useLocation } from 'react-router-dom';
import { Sparkles, UserRound, Menu, X, ShieldCheck } from 'lucide-react';
import { useEffect, useState } from 'react';
import { supabase } from '../lib/supabase';
import type { Bootstrap } from '../lib/types';
export function Brand(){return <Link to="/" className="brand" aria-label="Fitted home"><span className="brand-mark"><Sparkles size={17}/></span><span>Fitted</span></Link>}
export default function AppShell({children,bootstrap}:{children:React.ReactNode;bootstrap:Bootstrap|null}){const [open,setOpen]=useState(false);const [auth,setAuth]=useState(false);const location=useLocation();useEffect(()=>{supabase.auth.getSession().then(({data})=>setAuth(!!data.session));const {data}=supabase.auth.onAuthStateChange((_e,s)=>setAuth(!!s));return()=>data.subscription.unsubscribe()},[]);useEffect(()=>setOpen(false),[location.pathname]);return <><header className="site-header"><div className="nav-wrap"><Brand/><nav className={open?'nav-links open':'nav-links'}><NavLink to="/try-on">Try on</NavLink><NavLink to="/pricing">Pricing</NavLink>{auth&&<NavLink to="/dashboard">Dashboard</NavLink>}{bootstrap?.session.role&&['admin','super_admin','support_admin'].includes(bootstrap.session.role)&&<NavLink to="/admin"><ShieldCheck size={15}/> Admin</NavLink>}</nav><div className="nav-actions">{auth?<Link className="icon-link" to="/dashboard" aria-label="Account"><UserRound size={19}/></Link>:<><Link className="text-link desktop-only" to="/login">Log in</Link><Link className="button small" to="/try-on">Try free</Link></>}<button className="menu-btn" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div></div></header><main>{children}</main><footer className="site-footer"><div><Brand/><p>AI-powered virtual try-on, built for a faster way to explore style.</p></div><div className="footer-links"><Link to="/pricing">Pricing</Link><Link to="/try-on">Try on</Link><a href="mailto:support@example.com">Support</a></div><div className="footer-bottom">© {new Date().getFullYear()} Fitted. Privacy-first AI try-on.</div></footer></>}
